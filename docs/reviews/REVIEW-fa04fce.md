# 代码审查报告：commit `fa04fce`（PR #9 Cor 21 独立效应乱序撤销）

- **审查对象**：`fa04fceaa92a77b93acce4a4c3dff155578d7f71` + docs `bd36850`，3 文件，+48/-3 行（核心仅为 `context.rs` +43 行测试）
- **审查日期**：2026-08-16（仓库时区）
- **验证手段**：`cargo test -p cordis-core` **55 单元全绿**（新增 `cor21_independent_effects_revert_in_any_permutation`）、property 套件通过、`RUSTFLAGS="-D warnings" cargo clippy --workspace --all-targets -- -D warnings` 干净

---

## 结论：无 major、无 minor——纯测试固化，质量高

### 实现核对（全部正确）

- **Def 19 独立性的建模论证充分**：测试注释明确论证"不同键的 `set` 效应——变换只动自己的键（可交换）、逆只撤自己的键（互不干扰）"，与 Cor 21 的"两两独立"前提严格对应，无语义夸大。
- **排列覆盖合理**：4 种排列（`[4,3,2,1]` LIFO、`[1,2,3,4]` 正序、两个交错乱序）覆盖了"LIFO vs 非 LIFO、正序 vs 乱序"两个正交维度；注释点明"LIFO 只是其中一种排列（Thm 16 无需独立性），独立性买来的是其余所有排列"——对 Cor 21 与 Thm 16 的关系表述准确。
- **每步中间态断言（Thm 20(1)）**：撤销每步后断言剩余绑定数 `4 - undone`——"逆只撤自己的贡献"逐步骤验证，不只是最终态断言。
- **实现细节**：`Vec<Option<Disposer>>` + `take()` 处理 `FnOnce` 的任意顺序调用——惯用正确；断言消息含排列与步数（`{order:?}` / `{undone}`，Rust 1.95 inline format）利于定位。
- **文档**：THEORY-MAP 定理表标注完成（含独立性论证一句话）、已知偏差记录 1 条、PLAN §6/§9/里程碑三处同步。

### ⚪ 细节（可忽略）

1. **排列为 4 个代表而非全 24 种**：4 个独立效应下其余排列逻辑等价，代表选取合理；若要极尽严谨可全排列（4! 也廉价），非必需。
2. **测试位于 `context.rs` tests 模块**：复用了 `bind_effect`/`KeyK1`/`KeyK2` 既有辅助，位置合理。

---

## 总结

- **必须修复**：无。
- **建议修复**：无。
- **nit**：1–2 可忽略。

**置信度**：高——测试语义与 Cor 21/Thm 20/Def 19 的对应关系经逐条核对；测试运行实测通过。
