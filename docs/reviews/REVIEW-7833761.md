# 代码审查报告：commit `7833761`（P-3 P3-2+P3-3）

- **审查对象**：`7833761b318d4f2327fc08847fddd63afbe6cad5` — P-3 P3-2（advance guard 复核）+ P3-3（wasm 统一驱动回路）
- **审查日期**：2026-08-22
- **审查人**：independent-review-agent（委派）

---

## 总体结论

✅ **PASS WITH NITS**（0 Major / 0 Minor / 2 Nit）— P3-2/P3-3 达成，放行 P3-4。

- **Major 0 / Minor 0 / Nit 2**

## 核查（逐点命中）

### P3-2 advance guard 复核（REVIEW-8589ca2 m-2）✅
- `update_during_suspend_reclaims_and_restarts`（runtime.rs）：挂起中 `update_fiber` → unload 先收账（挂起集撤销）→ 惯性 reload 新代（又产 Await → **重新登记挂起集**）→ 断言新代挂起 + 挂起集含 + `k1` 重绑定 + `advance` 恢复完成 + 退役静止——**直证 guard=`target.is_some()` 弱化在当前更新路径下安全**（更新经 unload 先收账 resumable；静态复核确认与实现一致）。
- 复用 P3-1 的 `suspended_fibers()`/`advance_suspended`（runtime.rs:353/363 已存在）——方法引用正确。

### P3-3 wasm 统一驱动回路 ✅
- `WasmComponent::poll_and_advance(&self, runtime)`：`poll_remotes`（回填落位）→ `runtime.advance_suspended(|_| true)`（恢复挂起）——**端到端 `poll_and_advance_drives_suspend_loop`**（挂起 → `poll_and_advance` 循环 → guest 自取完成 → `probe` 落盘）**实测 3/3**（lib 已含，a2_e2e 3/3）。
- 语义：单 wasm 组件场景"全部恢复"；多组件编排用 `advance_suspended` 自带 judge（doc 已注明）。

### 判据 v2 评估 ✅（转 P3-4 记录）
- 本线**未实现**判据 v2（计划 §1.3：保持外部判据 + 显式批量驱动已覆盖；自动轮询不启用）——**确认合规**，评估结论由 P3-4 EXIT 记录。

### 门禁与额度 ✅
- `cargo +1.97.0 test -p cordis-core --lib` = **60/60**；`-p cordis-wasm --test a2_e2e` = **3/3**（父会话已验 wasm 全套 + clippy/fmt）；workspace 无回归。
- **core 额度**：本 commit 对 `crates/cordis-core` 仅 +36 行**测试**（`update_during_suspend_reclaims_and_restarts`）——**零新 core 行为改动**（suspended_fibers/advance_suspended 属 P3-1）；wasm 仅新增 `poll_and_advance` 方法。额度合规（P-3 授权范围内，无扩面）。

## 发现（Nit，不阻塞）

- **Nit-1**：`poll_and_advance`（`|_| true` 全恢复）在多组件挂起但某组件未就绪时，会强制 advance（try_execute 再遇 Await → 再挂起）——**无害**（再挂起），仅效率略低；doc 已注明"多组件用 judge"。观察项。
- **Nit-2**：端到端环路用 4000×1ms sleep 上限——与本仓库 async 时序测试既有模式（512×1ms）一致，无 flaky 风险（实测 1.04s 完成）。

## 结论

**P3-2/P3-3 达成**：advance guard 复核（update 挂起交互直证安全）+ wasm 统一驱动回路（agent 插件 P-5 的 await 驱动底座）——门禁全绿、core 额度合规、判据 v2 评估结论转 P3-4。

**放行 P3-4**（判据 v2 评估记录 + EXIT + 出口走查）。
